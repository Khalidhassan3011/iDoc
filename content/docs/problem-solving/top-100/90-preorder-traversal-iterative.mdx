---
title: 90. Preorder Traversal Without Recursion
description: Traverse binary tree in preorder without recursion
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { LanguageSwitcher, TranslatedText } from '@/components/LanguageSwitcher';

<LanguageSwitcher />

# <TranslatedText en="Preorder Traversal Without Recursion" bn="রিকার্শন ছাড়া প্রিঅর্ডার ট্রাভার্সাল" />

<TranslatedText en="Implement preorder traversal iteratively using a stack." bn="একটি স্ট্যাক ব্যবহার করে প্রিঅর্ডার ট্রাভার্সাল পুনরাবৃত্তিমূলকভাবে বাস্তবায়ন করুন।" />

## <TranslatedText en="Solution" bn="সমাধান" />

<TranslatedText en={<p>Use a stack to simulate the recursive call stack. Process root first, then push right and left children.</p>} bn={<p>রিকার্সিভ কল স্ট্যাক অনুকরণ করতে একটি স্ট্যাক ব্যবহার করুন। প্রথমে রুট প্রসেস করুন, তারপর ডান এবং বাম চিলড্রেন পুশ করুন।</p>} />

<Tabs items={['C++', 'Python', 'JavaScript']} groupId="language">
  <Tab value="C++">
```cpp
#include <iostream>
#include <vector>
#include <stack>
using namespace std;

struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

vector<int> preorderTraversal(TreeNode* root) {
    vector<int> result;
    if (!root) return result;

    stack<TreeNode*> st;
    st.push(root);

    while (!st.empty()) {
        TreeNode* node = st.top();
        st.pop();
        result.push_back(node->val);

        // Push right first so left is processed first
        if (node->right) st.push(node->right);
        if (node->left) st.push(node->left);
    }

    return result;
}

int main() {
    TreeNode* root = new TreeNode(1);
    root->left = new TreeNode(2);
    root->right = new TreeNode(3);
    root->left->left = new TreeNode(4);
    root->left->right = new TreeNode(5);

    vector<int> result = preorderTraversal(root);
    cout << "Preorder: ";
    for (int val : result) cout << val << " ";
    cout << endl;

    return 0;
}
```

**Output:**
```
Preorder: 1 2 4 5 3
```
  </Tab>
  <Tab value="Python">
```python
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

def preorder_traversal(root):
    if not root:
        return []

    result = []
    stack = [root]

    while stack:
        node = stack.pop()
        result.append(node.val)

        if node.right:
            stack.append(node.right)
        if node.left:
            stack.append(node.left)

    return result

root = TreeNode(1)
root.left = TreeNode(2)
root.right = TreeNode(3)
root.left.left = TreeNode(4)
root.left.right = TreeNode(5)

print("Preorder:", preorder_traversal(root))
```

**Output:**
```
Preorder: [1, 2, 4, 5, 3]
```
  </Tab>
  <Tab value="JavaScript">
```javascript
class TreeNode {
    constructor(val, left = null, right = null) {
        this.val = val;
        this.left = left;
        this.right = right;
    }
}

function preorderTraversal(root) {
    if (!root) return [];

    const result = [];
    const stack = [root];

    while (stack.length > 0) {
        const node = stack.pop();
        result.push(node.val);

        if (node.right) stack.push(node.right);
        if (node.left) stack.push(node.left);
    }

    return result;
}

const root = new TreeNode(1);
root.left = new TreeNode(2);
root.right = new TreeNode(3);
root.left.left = new TreeNode(4);
root.left.right = new TreeNode(5);

console.log("Preorder:", preorderTraversal(root));
```

**Output:**
```
Preorder: [1, 2, 4, 5, 3]
```
  </Tab>
</Tabs>

<TranslatedText en={<><p><strong>Time Complexity:</strong> O(n)</p><p><strong>Space Complexity:</strong> O(h)</p></>} bn={<><p><strong>সময় জটিলতা:</strong> O(n)</p><p><strong>স্থান জটিলতা:</strong> O(h)</p></>} />
