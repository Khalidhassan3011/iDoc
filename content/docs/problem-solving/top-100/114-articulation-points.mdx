---
title: 114. Articulation Points in Graph
description: Find articulation points (cut vertices) in an undirected graph
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { LanguageSwitcher, TranslatedText } from '@/components/LanguageSwitcher';

<LanguageSwitcher />

# <TranslatedText en="Articulation Points in Graph" bn="গ্রাফে আর্টিকুলেশন পয়েন্ট" />

<TranslatedText en="Find all articulation points - vertices whose removal increases the number of connected components." bn="সব আর্টিকুলেশন পয়েন্ট খুঁজুন - যে শীর্ষবিন্দুগুলি অপসারণ করলে সংযুক্ত উপাদানের সংখ্যা বৃদ্ধি পায়।" />

## <TranslatedText en="Solution" bn="সমাধান" />

<Tabs items={['Python']} groupId="language">
  <Tab value="Python">
```python
def find_articulation_points(graph, n):
    visited = [False] * n
    disc = [0] * n
    low = [0] * n
    parent = [-1] * n
    ap = [False] * n
    time = [0]

    def dfs(u):
        children = 0
        visited[u] = True
        disc[u] = low[u] = time[0]
        time[0] += 1

        for v in graph[u]:
            if not visited[v]:
                parent[v] = u
                children += 1
                dfs(v)

                low[u] = min(low[u], low[v])

                if parent[u] == -1 and children > 1:
                    ap[u] = True

                if parent[u] != -1 and low[v] >= disc[u]:
                    ap[u] = True

            elif v != parent[u]:
                low[u] = min(low[u], disc[v])

    for i in range(n):
        if not visited[i]:
            dfs(i)

    return [i for i in range(n) if ap[i]]

# Example
graph = {0: [1, 2], 1: [0, 2], 2: [0, 1, 3], 3: [2]}
print("Articulation points:", find_articulation_points(graph, 4))
```

**Output:**
```
Articulation points: [2]
```
  </Tab>
</Tabs>

<TranslatedText en={<><p><strong>Time Complexity:</strong> O(V + E)</p><p><strong>Space Complexity:</strong> O(V)</p></>} bn={<><p><strong>সময় জটিলতা:</strong> O(V + E)</p><p><strong>স্থান জটিলতা:</strong> O(V)</p></>} />
