---
title: 8. Convert Number to Words
description: Learn how to convert numbers into their English word representation
---

import { LanguageSwitcher, TranslatedText } from '@/components/LanguageSwitcher';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';

<LanguageSwitcher />

# <TranslatedText en="Convert Number to Words (English)" bn="সংখ্যা থেকে শব্দে রূপান্তর করুন (ইংরেজি)" />

## <TranslatedText en="Problem Statement" bn="সমস্যার বিবৃতি" />

<TranslatedText
  en={
    <>
      <p>Write a program that converts a number into its English word representation.</p>
      <p><strong>Input:</strong> <code>121</code></p>
      <p><strong>Output:</strong> <code>"one hundred twenty-one"</code></p>
      <p><strong>Examples:</strong></p>
      <ul>
        <li>5 → "five"</li>
        <li>25 → "twenty-five"</li>
        <li>1000 → "one thousand"</li>
        <li>1234 → "one thousand two hundred thirty-four"</li>
      </ul>
    </>
  }
  bn={
    <>
      <p>একটি প্রোগ্রাম লিখুন যা একটি সংখ্যাকে তার ইংরেজি শব্দ প্রতিনিধিত্বে রূপান্তর করে।</p>
      <p><strong>ইনপুট:</strong> <code>121</code></p>
      <p><strong>আউটপুট:</strong> <code>"one hundred twenty-one"</code></p>
      <p><strong>উদাহরণ:</strong></p>
      <ul>
        <li>5 → "five"</li>
        <li>25 → "twenty-five"</li>
        <li>1000 → "one thousand"</li>
        <li>1234 → "one thousand two hundred thirty-four"</li>
      </ul>
    </>
  }
/>

---

## <TranslatedText en="Approach 1: Simple Method (0-999)" bn="পদ্ধতি ১: সরল পদ্ধতি (0-999)" />

<TranslatedText
  en="Convert numbers from 0 to 999 using arrays for ones, tens, and hundreds."
  bn="ones, tens এবং hundreds এর জন্য অ্যারে ব্যবহার করে 0 থেকে 999 পর্যন্ত সংখ্যা রূপান্তর করুন।"
/>

<Tabs items={['C++', 'Python', 'JavaScript']}>
<Tab value="C++">
```cpp
#include <iostream>
#include <string>
using namespace std;

string ones[] = {"", "one", "two", "three", "four", "five", "six",
                 "seven", "eight", "nine"};

string teens[] = {"ten", "eleven", "twelve", "thirteen", "fourteen",
                  "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"};

string tens[] = {"", "", "twenty", "thirty", "forty", "fifty",
                 "sixty", "seventy", "eighty", "ninety"};

string convertToWords(int num) {
    if (num == 0) return "zero";

    string result = "";

    // Handle hundreds
    if (num >= 100) {
        result += ones[num / 100] + " hundred";
        num %= 100;
        if (num > 0) result += " ";
    }

    // Handle tens and ones
    if (num >= 20) {
        result += tens[num / 10];
        num %= 10;
        if (num > 0) result += "-" + ones[num];
    } else if (num >= 10) {
        result += teens[num - 10];
    } else if (num > 0) {
        result += ones[num];
    }

    return result;
}

int main() {
    int numbers[] = {0, 5, 25, 121, 500, 999};

    for (int num : numbers) {
        cout << num << " -> \"" << convertToWords(num) << "\"" << endl;
    }

    return 0;
}
```
</Tab>

<Tab value="Python">
```python
ones = ["", "one", "two", "three", "four", "five", "six",
        "seven", "eight", "nine"]

teens = ["ten", "eleven", "twelve", "thirteen", "fourteen",
         "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"]

tens = ["", "", "twenty", "thirty", "forty", "fifty",
        "sixty", "seventy", "eighty", "ninety"]

def convert_to_words(num):
    if num == 0:
        return "zero"

    result = ""

    # Handle hundreds
    if num >= 100:
        result += ones[num // 100] + " hundred"
        num %= 100
        if num > 0:
            result += " "

    # Handle tens and ones
    if num >= 20:
        result += tens[num // 10]
        num %= 10
        if num > 0:
            result += "-" + ones[num]
    elif num >= 10:
        result += teens[num - 10]
    elif num > 0:
        result += ones[num]

    return result

# Test
numbers = [0, 5, 25, 121, 500, 999]

for num in numbers:
    print(f'{num} -> "{convert_to_words(num)}"')
```
</Tab>

<Tab value="JavaScript">
```javascript
const ones = ["", "one", "two", "three", "four", "five", "six",
              "seven", "eight", "nine"];

const teens = ["ten", "eleven", "twelve", "thirteen", "fourteen",
               "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"];

const tens = ["", "", "twenty", "thirty", "forty", "fifty",
              "sixty", "seventy", "eighty", "ninety"];

function convertToWords(num) {
    if (num === 0) return "zero";

    let result = "";

    // Handle hundreds
    if (num >= 100) {
        result += ones[Math.floor(num / 100)] + " hundred";
        num %= 100;
        if (num > 0) result += " ";
    }

    // Handle tens and ones
    if (num >= 20) {
        result += tens[Math.floor(num / 10)];
        num %= 10;
        if (num > 0) result += "-" + ones[num];
    } else if (num >= 10) {
        result += teens[num - 10];
    } else if (num > 0) {
        result += ones[num];
    }

    return result;
}

// Test
const numbers = [0, 5, 25, 121, 500, 999];

numbers.forEach(num => {
    console.log(`${num} -> "${convertToWords(num)}"`);
});
```
</Tab>
</Tabs>

<TranslatedText
  en={
    <>
      <p><strong>Time Complexity:</strong> O(1) - Constant operations for numbers up to 999</p>
      <p><strong>Space Complexity:</strong> O(1) - Fixed size arrays</p>
      <p><strong>Range:</strong> Works for numbers 0 to 999</p>
    </>
  }
  bn={
    <>
      <p><strong>সময় জটিলতা:</strong> O(1) - 999 পর্যন্ত সংখ্যার জন্য ধ্রুবক অপারেশন</p>
      <p><strong>স্পেস জটিলতা:</strong> O(1) - স্থির আকারের অ্যারে</p>
      <p><strong>পরিসীমা:</strong> 0 থেকে 999 পর্যন্ত সংখ্যার জন্য কাজ করে</p>
    </>
  }
/>

---

## <TranslatedText en="Approach 2: Extended Version (Thousands)" bn="পদ্ধতি ২: বর্ধিত সংস্করণ (হাজার)" />

<TranslatedText
  en="Extend the solution to handle numbers up to 999,999 (including thousands)."
  bn="999,999 পর্যন্ত সংখ্যা (হাজার সহ) পরিচালনা করতে সমাধান সম্প্রসারিত করুন।"
/>

<Tabs items={['C++', 'Python', 'JavaScript']}>
<Tab value="C++">
```cpp
#include <iostream>
#include <string>
using namespace std;

string ones[] = {"", "one", "two", "three", "four", "five", "six",
                 "seven", "eight", "nine"};

string teens[] = {"ten", "eleven", "twelve", "thirteen", "fourteen",
                  "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"};

string tens[] = {"", "", "twenty", "thirty", "forty", "fifty",
                 "sixty", "seventy", "eighty", "ninety"};

string convertHundreds(int num) {
    string result = "";

    if (num >= 100) {
        result += ones[num / 100] + " hundred";
        num %= 100;
        if (num > 0) result += " ";
    }

    if (num >= 20) {
        result += tens[num / 10];
        num %= 10;
        if (num > 0) result += "-" + ones[num];
    } else if (num >= 10) {
        result += teens[num - 10];
    } else if (num > 0) {
        result += ones[num];
    }

    return result;
}

string convertToWordsExtended(int num) {
    if (num == 0) return "zero";

    string result = "";

    // Handle thousands
    if (num >= 1000) {
        result += convertHundreds(num / 1000) + " thousand";
        num %= 1000;
        if (num > 0) result += " ";
    }

    // Handle remaining hundreds, tens, ones
    result += convertHundreds(num);

    return result;
}

int main() {
    int numbers[] = {121, 1000, 1234, 5678, 99999};

    for (int num : numbers) {
        cout << num << " -> \"" << convertToWordsExtended(num) << "\"" << endl;
    }

    return 0;
}
```
</Tab>

<Tab value="Python">
```python
ones = ["", "one", "two", "three", "four", "five", "six",
        "seven", "eight", "nine"]

teens = ["ten", "eleven", "twelve", "thirteen", "fourteen",
         "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"]

tens = ["", "", "twenty", "thirty", "forty", "fifty",
        "sixty", "seventy", "eighty", "ninety"]

def convert_hundreds(num):
    result = ""

    if num >= 100:
        result += ones[num // 100] + " hundred"
        num %= 100
        if num > 0:
            result += " "

    if num >= 20:
        result += tens[num // 10]
        num %= 10
        if num > 0:
            result += "-" + ones[num]
    elif num >= 10:
        result += teens[num - 10]
    elif num > 0:
        result += ones[num]

    return result

def convert_to_words_extended(num):
    if num == 0:
        return "zero"

    result = ""

    # Handle thousands
    if num >= 1000:
        result += convert_hundreds(num // 1000) + " thousand"
        num %= 1000
        if num > 0:
            result += " "

    # Handle remaining hundreds, tens, ones
    result += convert_hundreds(num)

    return result

# Test
numbers = [121, 1000, 1234, 5678, 99999]

for num in numbers:
    print(f'{num} -> "{convert_to_words_extended(num)}"')
```
</Tab>

<Tab value="JavaScript">
```javascript
const ones = ["", "one", "two", "three", "four", "five", "six",
              "seven", "eight", "nine"];

const teens = ["ten", "eleven", "twelve", "thirteen", "fourteen",
               "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"];

const tens = ["", "", "twenty", "thirty", "forty", "fifty",
              "sixty", "seventy", "eighty", "ninety"];

function convertHundreds(num) {
    let result = "";

    if (num >= 100) {
        result += ones[Math.floor(num / 100)] + " hundred";
        num %= 100;
        if (num > 0) result += " ";
    }

    if (num >= 20) {
        result += tens[Math.floor(num / 10)];
        num %= 10;
        if (num > 0) result += "-" + ones[num];
    } else if (num >= 10) {
        result += teens[num - 10];
    } else if (num > 0) {
        result += ones[num];
    }

    return result;
}

function convertToWordsExtended(num) {
    if (num === 0) return "zero";

    let result = "";

    // Handle thousands
    if (num >= 1000) {
        result += convertHundreds(Math.floor(num / 1000)) + " thousand";
        num %= 1000;
        if (num > 0) result += " ";
    }

    // Handle remaining hundreds, tens, ones
    result += convertHundreds(num);

    return result;
}

// Test
const numbers = [121, 1000, 1234, 5678, 99999];

numbers.forEach(num => {
    console.log(`${num} -> "${convertToWordsExtended(num)}"`);
});
```
</Tab>
</Tabs>

<TranslatedText
  en={
    <>
      <p><strong>Output Examples:</strong></p>
      <pre>
121 → "one hundred twenty-one"
1000 → "one thousand"
1234 → "one thousand two hundred thirty-four"
5678 → "five thousand six hundred seventy-eight"
99999 → "ninety-nine thousand nine hundred ninety-nine"
      </pre>
    </>
  }
  bn={
    <>
      <p><strong>আউটপুট উদাহরণ:</strong></p>
      <pre>
121 → "one hundred twenty-one"
1000 → "one thousand"
1234 → "one thousand two hundred thirty-four"
5678 → "five thousand six hundred seventy-eight"
99999 → "ninety-nine thousand nine hundred ninety-nine"
      </pre>
    </>
  }
/>

---

## <TranslatedText en="Approach 3: Recursive Solution (Scalable)" bn="পদ্ধতি ৩: রিকার্সিভ সমাধান (স্কেলেবল)" />

<TranslatedText
  en="Use recursion to handle millions and billions with a scalable approach."
  bn="একটি স্কেলেবল পদ্ধতির সাথে মিলিয়ন এবং বিলিয়ন পরিচালনা করতে রিকার্শন ব্যবহার করুন।"
/>

<Tabs items={['C++', 'Python', 'JavaScript']}>
<Tab value="C++">
```cpp
#include <iostream>
#include <string>
using namespace std;

string ones[] = {"", "one", "two", "three", "four", "five", "six",
                 "seven", "eight", "nine"};

string teens[] = {"ten", "eleven", "twelve", "thirteen", "fourteen",
                  "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"};

string tens[] = {"", "", "twenty", "thirty", "forty", "fifty",
                 "sixty", "seventy", "eighty", "ninety"};

string thousands[] = {"", "thousand", "million", "billion"};

string convertBelowThousand(int num) {
    if (num == 0) return "";

    string result = "";

    if (num >= 100) {
        result += ones[num / 100] + " hundred";
        num %= 100;
        if (num > 0) result += " ";
    }

    if (num >= 20) {
        result += tens[num / 10];
        num %= 10;
        if (num > 0) result += "-" + ones[num];
    } else if (num >= 10) {
        result += teens[num - 10];
    } else if (num > 0) {
        result += ones[num];
    }

    return result;
}

string numberToWords(int num) {
    if (num == 0) return "zero";

    string result = "";
    int groupIndex = 0;

    while (num > 0) {
        if (num % 1000 != 0) {
            string groupStr = convertBelowThousand(num % 1000);
            if (thousands[groupIndex] != "") {
                groupStr += " " + thousands[groupIndex];
            }

            if (result != "") {
                result = groupStr + " " + result;
            } else {
                result = groupStr;
            }
        }

        num /= 1000;
        groupIndex++;
    }

    return result;
}

int main() {
    int numbers[] = {0, 121, 1234, 1000000, 1234567890};

    for (int num : numbers) {
        cout << num << " -> \"" << numberToWords(num) << "\"" << endl;
    }

    return 0;
}
```
</Tab>

<Tab value="Python">
```python
ones = ["", "one", "two", "three", "four", "five", "six",
        "seven", "eight", "nine"]

teens = ["ten", "eleven", "twelve", "thirteen", "fourteen",
         "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"]

tens = ["", "", "twenty", "thirty", "forty", "fifty",
        "sixty", "seventy", "eighty", "ninety"]

thousands = ["", "thousand", "million", "billion"]

def convert_below_thousand(num):
    if num == 0:
        return ""

    result = ""

    if num >= 100:
        result += ones[num // 100] + " hundred"
        num %= 100
        if num > 0:
            result += " "

    if num >= 20:
        result += tens[num // 10]
        num %= 10
        if num > 0:
            result += "-" + ones[num]
    elif num >= 10:
        result += teens[num - 10]
    elif num > 0:
        result += ones[num]

    return result

def number_to_words(num):
    if num == 0:
        return "zero"

    result = ""
    group_index = 0

    while num > 0:
        if num % 1000 != 0:
            group_str = convert_below_thousand(num % 1000)
            if thousands[group_index] != "":
                group_str += " " + thousands[group_index]

            if result != "":
                result = group_str + " " + result
            else:
                result = group_str

        num //= 1000
        group_index += 1

    return result

# Test
numbers = [0, 121, 1234, 1000000, 1234567890]

for num in numbers:
    print(f'{num} -> "{number_to_words(num)}"')
```
</Tab>

<Tab value="JavaScript">
```javascript
const ones = ["", "one", "two", "three", "four", "five", "six",
              "seven", "eight", "nine"];

const teens = ["ten", "eleven", "twelve", "thirteen", "fourteen",
               "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"];

const tens = ["", "", "twenty", "thirty", "forty", "fifty",
              "sixty", "seventy", "eighty", "ninety"];

const thousands = ["", "thousand", "million", "billion"];

function convertBelowThousand(num) {
    if (num === 0) return "";

    let result = "";

    if (num >= 100) {
        result += ones[Math.floor(num / 100)] + " hundred";
        num %= 100;
        if (num > 0) result += " ";
    }

    if (num >= 20) {
        result += tens[Math.floor(num / 10)];
        num %= 10;
        if (num > 0) result += "-" + ones[num];
    } else if (num >= 10) {
        result += teens[num - 10];
    } else if (num > 0) {
        result += ones[num];
    }

    return result;
}

function numberToWords(num) {
    if (num === 0) return "zero";

    let result = "";
    let groupIndex = 0;

    while (num > 0) {
        if (num % 1000 !== 0) {
            let groupStr = convertBelowThousand(num % 1000);
            if (thousands[groupIndex] !== "") {
                groupStr += " " + thousands[groupIndex];
            }

            if (result !== "") {
                result = groupStr + " " + result;
            } else {
                result = groupStr;
            }
        }

        num = Math.floor(num / 1000);
        groupIndex++;
    }

    return result;
}

// Test
const numbers = [0, 121, 1234, 1000000, 1234567890];

numbers.forEach(num => {
    console.log(`${num} -> "${numberToWords(num)}"`);
});
```
</Tab>
</Tabs>

<TranslatedText
  en={
    <>
      <p><strong>Output Examples:</strong></p>
      <pre>
0 → "zero"
121 → "one hundred twenty-one"
1234 → "one thousand two hundred thirty-four"
1000000 → "one million"
1234567890 → "one billion two hundred thirty-four million five hundred sixty-seven thousand eight hundred ninety"
      </pre>
      <p><strong>Time Complexity:</strong> O(log n) - Divide by 1000 in each iteration</p>
      <p><strong>Space Complexity:</strong> O(1) - Constant space for arrays</p>
      <p><strong>Range:</strong> Handles numbers up to billions (2³¹ - 1)</p>
    </>
  }
  bn={
    <>
      <p><strong>আউটপুট উদাহরণ:</strong></p>
      <pre>
0 → "zero"
121 → "one hundred twenty-one"
1234 → "one thousand two hundred thirty-four"
1000000 → "one million"
1234567890 → "one billion two hundred thirty-four million five hundred sixty-seven thousand eight hundred ninety"
      </pre>
      <p><strong>সময় জটিলতা:</strong> O(log n) - প্রতিটি পুনরাবৃত্তিতে 1000 দ্বারা ভাগ</p>
      <p><strong>স্পেস জটিলতা:</strong> O(1) - অ্যারের জন্য ধ্রুবক স্পেস</p>
      <p><strong>পরিসীমা:</strong> বিলিয়ন পর্যন্ত সংখ্যা পরিচালনা করে (2³¹ - 1)</p>
    </>
  }
/>

---

## <TranslatedText en="Comparison of Approaches" bn="পদ্ধতির তুলনা" />

| Approach | Max Range | Complexity | Best For |
|----------|-----------|------------|----------|
| Simple (0-999) | 999 | O(1) | Quick interviews, basic problems |
| Extended (Thousands) | 999,999 | O(1) | **Most interview scenarios** |
| Recursive (Scalable) | 2 billion+ | O(log n) | Complete solution, production code |

---

## <TranslatedText en="Interview Tips" bn="ইন্টারভিউ টিপস" />

<TranslatedText
  en={
    <>
      <ol>
        <li><strong>Clarify range:</strong> Ask what range of numbers to support (0-999, millions, etc.)</li>
        <li><strong>Edge cases:</strong> Handle 0, teens (11-19), and exact multiples (100, 1000)</li>
        <li><strong>Start simple:</strong> Begin with 0-999 solution, then extend if needed</li>
        <li><strong>Code organization:</strong> Break into helper functions (convertHundreds, convertBelowThousand)</li>
        <li><strong>Hyphenation:</strong> Clarify if "twenty-one" vs "twenty one" format is required</li>
        <li><strong>Negative numbers:</strong> Discuss whether to handle negatives (prepend "negative")</li>
        <li><strong>Testing:</strong> Test edge cases: 0, 10-19, 20, 100, 1000</li>
      </ol>
    </>
  }
  bn={
    <>
      <ol>
        <li><strong>পরিসীমা স্পষ্ট করুন:</strong> জিজ্ঞাসা করুন কোন পরিসীমার সংখ্যা সমর্থন করতে হবে (0-999, মিলিয়ন, ইত্যাদি)</li>
        <li><strong>প্রান্তিক ক্ষেত্র:</strong> 0, teens (11-19), এবং সঠিক গুণিতক (100, 1000) পরিচালনা করুন</li>
        <li><strong>সহজ দিয়ে শুরু করুন:</strong> 0-999 সমাধান দিয়ে শুরু করুন, তারপর প্রয়োজনে সম্প্রসারিত করুন</li>
        <li><strong>কোড সংগঠন:</strong> হেল্পার ফাংশনে ভাগ করুন (convertHundreds, convertBelowThousand)</li>
        <li><strong>হাইফেনেশন:</strong> "twenty-one" বনাম "twenty one" ফরম্যাট প্রয়োজন কিনা তা স্পষ্ট করুন</li>
        <li><strong>ঋণাত্মক সংখ্যা:</strong> ঋণাত্মক পরিচালনা করতে হবে কিনা তা আলোচনা করুন ("negative" যুক্ত করুন)</li>
        <li><strong>পরীক্ষা:</strong> প্রান্তিক ক্ষেত্র পরীক্ষা করুন: 0, 10-19, 20, 100, 1000</li>
      </ol>
    </>
  }
/>
