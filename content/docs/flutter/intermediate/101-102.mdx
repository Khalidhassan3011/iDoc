---
title: Intermediate Flutter Questions (Questions 101-102)
description: Intermediate Flutter Questions (Questions 101-102)
---

# Intermediate Flutter Questions (Questions 101-102)

> **Note:**
  **Questions 101-102 of 102**

  All questions consolidated from multiple sources with source attribution.

---

## 101. What's the purpose of the build() method in Flutter?

**📚 LEMON**

The `build()` method describes how to display the widget using a tree of other widgets. It's called every time the UI needs to be updated. The method returns a widget tree that represents the visual structure of your widget.

---

## 102. When do you use release mode and what's the function of release mode?

**📚 TURINGQUESTIONS**

A **release mode** is used when one needs to optimize and produce the codes without any debug data. The release mode is used for deploying the app.

The command used to compile the release mode is:
```bash
flutter run --release
```

---